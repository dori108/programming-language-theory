리베이스란? 두개의 브랜치를 합치는 방법
커밋기록 재배치 > 브랜치 이력을 깔끔히 유지 가능

명령어
  git rebase 브랜치 이름 : 현재 브랜치의 커밋 재배치
  git rebase --abort : 리베이스 중단 > 기존의 상태로 되돌린다.
  git rebase --continue : 충돌해결

리베이스의 장단점
1. 장점
깔끔한 히스토리 유지
병합없이 진행 가능
2. 단점
리베이스 중에 충돌이 발생가능 > 충돌 해결과정이 까다로
커밋기록이 변경되기 때문에 해쉬값등 중요 정보의 변화가 많다
