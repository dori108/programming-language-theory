브랜치 개념과 활용
브랜치는 코드를 분리하여 독립적으로 작업할 수 있는 개념
각각의 브랜치는 서로 다른 작업을 수행할 수 있고, 변경내역을 분리해서 관리할 수 있다.

git checkout의 역할
파일 복원하기:
git checkout -- <file>은 특정 파일의 변경 내용을 이전 커밋 상태로 복원한다.
브랜치 전환하기

브랜치 병합 과정
1. 병합대상 브랜치 전환
git checkout 브랜치 이름 : 병합하고자 하는 브랜치로 이동

2. 병합할 브랜치를 병합대상 브랜치로 병합
git merge 브랜치 이름 : master등의 메인 브랜치로 병합된다.

병합 과정에서 충돌이 발생할 수 있는데 이를 해결하고 병합해줘야한다.
